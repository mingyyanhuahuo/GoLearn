# 开发日程与学习过程(Development Journal)

> 项目:精弘网络大作业 · Go 后端论坛(11 个接口 + 4 项进阶)
> 周期:**2026-08-17 ~ 2026-08-25,共 9 天**,从第一天(8/17)到交付日(8/25)
> 全部日期来自文件时间戳与 git 提交历史,真实可查;无记录的日子如实标注,不虚构。

---

## 一、日程总览(9 天)

| 日期 | 阶段 | 当天完成的内容 | 证据 |
|---|---|---|---|
| 08-17 | **第一天** | 用熟悉的 Node.js(Express)按大作业规格写出完整参考实现:11 接口 + 4 项进阶,当天完成 | `大作业-nodejs/`(10:45 → 14:35) |
| 08-18 | 学习第 1 天 | Gin 框架三个练习:hello_gin(路由/绑定/JSON)、注册接口(binding 校验 + 统一响应)、骨架 | `1/1` `1/2` `1/3` |
| 08-19 | 学习第 2 天 | 中间件 + JWT 概念学习(无独立练习文件) | 无文件记录 |
| 08-20 | 学习第 3 天(前) | 继续完善注册练习(1/2 于 14:41 修改) | `1/2/main.go` |
| 08-21 | 学习第 3+4 天 | Gorm 练习(`3/1`:model/dao/密码哈希);**4/1 项目启动**:连接数据库 + 注册/登录 + 4 张表设计,git 首次提交并推送 GitHub | `3/1/`、git `0a45cfd` `bd5b810` `cf4f27a` |
| 08-22 ~ 08-23 | 学习第 5 天 | 4/1 剩余接口开发(详情/点赞/批量/管理员删帖),开发期间未逐个提交,8/24 凌晨统一提交 | git `a91553e` |
| 08-24 | 学习第 6+8 天 | ① `6/` 进阶练习项目(BizError/错误中间件/zap 日志雏形,11:23~11:38);② 热门排序公式落地 + 进阶① 合入 4/1(14:35) | `6/`、git `ef7a74c` |
| 08-25 | 冲刺第 9+10 天 | ① 11:33 Redis 点赞链路(异步落库 + 对账 + 优雅关闭);② 22:57 **Agent 聊天接口**(接口 11);③ 22:59 README + .gitignore;④ 深夜逐条推送,交付 | git `cc655c5` `9877c23` `542d3bc` `bfc153c` |

---

## 二、完整学习过程(每天详记)

### 📅 08-17(第一天):先写一份"答案"——Node.js 参考实现

- **为什么**:大作业要求 Go,但当时 Go 还是 0 基础。策略:先用熟悉的 Node.js(Express)按规格完整实现一遍,把规格里的每个接口、每个响应格式、每项进阶要求**先跑通、先看懂**,之后用 Go 重写时就有了一份可对照的"标准答案"。
- **做了什么**(10:45 开始,14:35 完成,约 4 小时):
  - 11 个接口全部实现:`auth(注册/登录)`、`posts(发帖/列表/详情/删除/点赞/批量/评论)`、`admin(删任意帖)`、`agent(选做)`
  - 4 项进阶全部实现:`errorHandler`(全局错误)+ `logger`(winston 日志轮转)、`agentService`(session 隔离 + Tool + 草稿确认)、`likeService`(Redis 点赞落库,未配 Redis 自动降级内存)、`hotService`(时间衰减)
  - 产物:`大作业-nodejs/`(完整工程 + 冒烟测试脚本)
- **收获**:大作业"长什么样"从文字变成了能跑的东西;后续 Go 版所有接口行为(响应格式、草稿确认流程、点赞幂等)都以它为参照。

### 📅 08-18(学习第 1 天):Gin 框架三连练

按计划「第 1 天:Gin 框架精通(指南 3.1~3.5)」,产出 3 个零件练习:

| 练习 | 内容 | 学到 |
|---|---|---|
| `1/1` hello_gin(11:07) | `GET/POST /posts`,内存数组读写,`binding:"required"` 参数校验 | 路由注册、JSON 请求绑定、错误响应 |
| `1/2` 注册接口(12:33) | `RegisterReq` 带全套校验:`required,min=6,max=20,alphanum`、`numeric,len=11`、`oneof=admin user`;`response` 统一响应包(19:47) | 参数校验体系、统一响应封装 |
| `1/3` 骨架(14:21) | `gin.Default()` + `Run(":8080")` | 最小可运行骨架 |

### 📅 08-19(学习第 2 天):中间件 + JWT(概念为主)

- 按计划「第 2 天:中间件 + JWT 鉴权(指南 3.6~3.8)」学习 Auth 中间件、JWT 签发/验证原理(HS256、Claims、Bearer 方案)。
- **无独立练习文件**——概念在 8/21 起的 4/1 项目里直接落地,不虚构当天产出。

### 📅 08-20(学习第 3 天前段):注册练习收尾

- 完善 `1/2` 注册练习(14:41 最后修改),把统一响应接入注册流程,为 4/1 的注册接口打样。

### 📅 08-21(学习第 3+4 天):Gorm 落地 + 项目启动(关键转折日)

**上午~傍晚(第 3 天)**:Gorm 与 MySQL 全家桶练习 `3/1`(11:56 → 21:30):

- `model/`(14:19):表结构映射
- `dao/`(14:09):数据访问层
- `hashcom/`(20:34):密码哈希工具
- `main.go`(21:30):串联运行

**晚上(第 4 天)**:**4/1 正式项目启动**,当天 3 次提交并首次推送 GitHub:

- `0a45cfd`(21:35):连接数据库 + 注册/登录功能 + users/posts/comments/likes 4 张表设计
- `bd5b810`(21:44):Initial commit
- `cf4f27a`(21:50):Merge,仓库建立
- 骨架(三层架构)按计划第 4 天完成,`4/1` 目录自此成为主线项目。

### 📅 08-22 ~ 08-23(学习第 5 天):10 接口开发

- 在 4/1 骨架上开发剩余接口:帖子详情(作者 + 评论列表)、点赞/取消(事务)、批量查赞(IN 查询)、管理员删帖(双中间件)。
- 两天开发期间未逐个提交,8/24 凌晨统一提交 `a91553e`(10 接口全绿)。

### 📅 08-24(学习第 6+8 天):进阶① + 进阶④

- **上午**(11:23~11:38):`6/` 练习项目——把进阶①(BizError、错误中间件、hashpassword)先在独立小项目里跑通。
- **下午**(14:35):`ef7a74c`——热门排序 sort=hot(时间衰减热度公式)+ 进阶① 整套合入 4/1:统一错误码 errcode、全局错误中间件、zap + lumberjack 日志轮转。
- 至此:基础 10 接口 + 进阶① + 进阶④ 完成,进度超前,第 7/8 天内容合并消化。

### 📅 08-25(冲刺第 9+10 天):Redis 链路 + Agent + 交付

- **11:33** `cc655c5`:热门排序完善 + **进阶③ Redis 点赞链路**——点赞实时走 Redis(SADD/SREM + 原子计数),`LikeSyncer` 后台协程定时落库 MySQL,对账归零保证最终一致,进程退出优雅关闭兜底。
- **22:57** `9877c23`:**进阶② Agent 聊天接口(接口 11,选做)**——需求原文为 Eino,按计划文档附录 B 降级授权,手写规则 Agent 实现同等能力:session 持久化(Redis,30 分钟滑动 TTL)、多轮续接(LastTool 记忆)、只读工具(get_posts / get_post_detail)、写操作草稿状态机(先存 Redis、confirm_draft_id 二次确认、确认即销毁防重复)。
- **22:57** `542d3bc`:.gitignore + 停止追踪 config.yaml(密码)/logs/exe。
- **22:59** `bfc153c`:README 完善(技术栈/启动/11 接口清单/四项进阶设计说明)。
- **深夜**:6 个 commit 逐条推送 GitHub,工作区干净,交付完成。

---

## 三、每日开发明细(git 提交历史,10 条)

| 时间 | commit | 内容 |
|---|---|---|
| 08-21 21:35 | `0a45cfd` | 连接数据库、注册/登录功能、post/comment/user/author/likeList 表设计 |
| 08-21 21:44 | `bd5b810` | Initial commit(项目初始化) |
| 08-21 21:50 | `cf4f27a` | Merge(首次推送,仓库建立) |
| 08-24 00:26 | `a91553e` | **基础版 10 接口**:详情(作者+评论)/点赞切换(事务)/批量查赞(IN 查询)/管理员删帖(双中间件) |
| 08-24 14:35 | `ef7a74c` | 热门排序 sort=hot(热度公式落地)+ 进阶① BizError/错误中间件/zap 日志 |
| 08-25 11:33 | `cc655c5` | 热门排序完善 + **Redis 点赞链路**:SADD/SREM 实时计数、LikeSyncer 异步落库、对账归零、优雅关闭 |
| 08-25 22:57 | `9877c23` | **Agent 聊天接口**(接口 11):session 持久化 + 多轮续接 + 草稿状态机 |
| 08-25 22:57 | `542d3bc` | .gitignore + 停止追踪 config.yaml/logs/exe(敏感文件不入库) |
| 08-25 22:59 | `bfc153c` | README 完善,交付 |
| 08-25 23:0x | `bb1cf53` | 本文档(development-journal) |

---

## 四、踩坑与心得(完整记录见《回顾与感想》)

### 最贵的坑:Windows 中文编码

Git Bash 下 curl 命令行中文按 GBK 发送 → 服务端 UTF-8 关键词匹配全部失败。解决:请求体写 UTF-8 文件 + `curl -d @文件`;Python 脚本用 `\uXXXX` 转义 + `sys.stdout.reconfigure(encoding="utf-8")`。

### 最隐蔽的坑:同类型参数互换

`ToggleLike(userId, postId)` 写成 `ToggleLike(postId, userId)`,编译器不报错,只有全流程 curl + 查表才能现形。

### 最有价值的认知:Agent 没有魔法

Agent = 意图判断(关键词规则)+ 工具调用(接口抽象)+ 状态记忆(Redis 持久化)。写操作必须二次确认:先存草稿、用户带 `confirm_draft_id` 确认后才落库,确认即销毁防重复——这是 AI 直接写库的安全边界。

### 全部问题记录

| 文件 | 内容 |
|---|---|
| `回顾与感想.md` | 第 4 / 5 / 10 天:每日问题表、排查修复表、感想 |
| `06-Agent学习笔记.md` | Agent 接口逐行注释 + 原理 + 常见坑 |
| `05-Redis点赞链路详解.md` | 点赞链路设计细节 |

---

## 五、四项进阶落地情况

| # | 进阶项 | 状态 | 落地要点 |
|---|---|---|---|
| ① | 全局错误处理 + 日志 | ✅ | errcode 统一错误码(错误自带状态码)、全局错误中间件、zap + lumberjack 日志轮转 |
| ② | Agent 服务(选做) | ✅ | 按计划附录 B 降级:手写规则 Agent 实现同等能力(详见 README 进阶设计说明) |
| ③ | Redis 点赞优化 | ✅ | 实时计数走 Redis,LikeSyncer 异步定时落库,进程退出优雅关闭兜底 |
| ④ | 热门排序 | ✅ | 时间衰减公式,`GET /posts?sort=hot` |

---

## 六、测试验证

- 全功能回归测试 **45/45 通过**(注册/登录/帖子/评论/点赞/删除/权限/admin/Agent)
- Agent 业务 curl 全流程通过:列表 → 多轮续接翻页 → 详情 → 草稿生成 → 二次确认落库 → 重复确认 404 → 未登录 401
- Node.js 参考实现自带冒烟测试(`大作业-nodejs/scripts/smoke.js`),Go 版与之行为对照一致
