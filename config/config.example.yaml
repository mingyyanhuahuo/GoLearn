# 配置模板示例(config.example.yaml)
# 本文件是"给人看的模板",不参与运行。
# 真正生效的配置是本地 config/config.yaml(已在 .gitignore,不进仓库)。
#
# 注意:下方 mysql 密码是开发早期的旧值,【密码已经改了[狗头]】,
# 该旧值已失效,新的数据库密码只存在于本地 config/config.yaml 中,
# 拿着旧值去连数据库是连不上的[狗头]。
#
# 运行方式:复制本文件为 config/config.yaml,再填入你自己的密码。

server:
    port: 8080
    host: localhost
mysql:
    host: localhost
    port: 3306
    user: root
    password: 1433223   # 密码已经改了[狗头],此旧值已失效
    database: day4database
    dsn: root:1433223@tcp(localhost:3306)/day4database?charset=utf8mb4&parseTime=True&loc=Local
redis:
    host: localhost
    port: 6379
    password:
jwt:
    secret: misakasayliumengyuyyds   # [狗头]
